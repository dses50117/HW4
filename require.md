0. 專案概念先講清楚

目標：
做一個 Streamlit 網頁，使用者輸入文字（中文 / 英文），按下按鈕 → 產生語音 → 網頁上可以播放 / 下載。

技術方向先假設用 Python + Streamlit + 開源 TTS：

簡單版：gTTS（Google Translate API 包裝，品質中等，但要連網）

進階版：Coqui TTS 或 🐸 TTS（本地模型，可調參數、可訓練）

最簡單測試：pyttsx3（離線，但聲音機器感較重）

下面就照 CRISP-DM 六大階段說。

1. Business Understanding（商業 / 問題理解）
1.1 你在解決什麼問題？

可能的需求（你可挑一個當主線）：

快速 Demo / 教學：給老師 / 同學示範 TTS 技術。

無障礙輔助：讓閱讀困難（視障、閱讀障礙）的人用文字變成語音。

內容創作工具：把文章、貼文、自動生成文案轉成語音（Podcast、旁白）。

1.2 成功標準（KPI）

可以定幾個指標：

使用體驗：

文字輸入 → 語音輸出時間 ≤ 3 秒（短句）

Web UI 操作步驟：1 輸入 → 2 選參數 → 3 產生語音

語音品質：

主觀評分（1～5 分），希望平均 ≥ 3.5

中英文都不會嚴重爆音 / 破音、速度自然

系統面：

不崩潰、不 timeout

最少支援：中文 + 英文，1 個女聲 / 1 個男聲（看選定的 TTS 引擎）

1.3 範圍與限制

範圍：

文字 → 語音（單向）

不做：長篇情緒合成、多說話者對話、分句斷詞 NLP 強化

限制：

部分 TTS 模型需要 GPU，部署成本高

使用第三方（例如 gTTS）有流量 / 速度 / 法規限制

部署位置（本機 / 雲端 / Kaggle / Hugging Face）會限制體驗

你可以寫成一個小專題報告開頭：

本專案目標是開發一個基於 Streamlit 的文字轉語音 Web 服務，提供簡單、低門檻的 TTS 體驗，作為未來擴充自有 TTS 模型的基礎平台。

2. Data Understanding（資料理解）

TTS 本質上是「文字 → 語音波形」，需要兩種資料：

文字資料（Text Corpus）

來源：你自己輸入的句子、預設範例文本（新聞、對話、FAQ）

功能：測試模型在不同長度、語言、標點符號下的表現

語音資料（Audio Corpus，用來訓練/微調）

如果你只用「現成 TTS 模型」，就不需要自己準備聲音資料

如果你要「自訂聲音」，就需要：

一個人錄音（同一聲線）：例如 1～2 小時的乾淨語音

對應的逐句文字（文字對齊）

2.1 如果你用現成模型（建議初期）

Data Understanding 會轉變為：
理解模型支援什麼語言 / 語者 / 參數 / 限制，例如：

支援中文嗎？

語速 / 音調 / 語者怎麼調？

推論時間 vs 文本長度大概是多少？

2.2 你需要蒐集什麼資料來測試？

可手工準備一個 test_sentences.csv：

id	language	text	type
1	zh	今天天氣很好，但是我還是想待在家裡。	daily casual
2	zh	本系統是一個基於 Streamlit 的文字轉語音工具。	technical
3	en	I think AI can replace some repetitive jobs, but not human emotions.	opinion
4	en	Okay but why did my brain suddenly remember something embarrassing from ten years ago?	super human

這些句子用來：

測試換行、標點符號、長度、語氣

感受語音自然度、停頓、斷句是否合理

3. Data Preparation（資料準備）

這裡的「資料準備」，主要是 文字清理 + 輸入格式轉換，而不是傳統的 numeric features。

3.1 文字前處理（Text Cleaning）

你可以先寫一個 clean_text() 函式：

去掉隱藏字元（\u200b 等）

移除多餘空白 / 重複換行

正規化符號：

全形數字 → 半形

奇怪的標點 → 正常標點（… → ...）

避免太長的字串（例如 > 300～500 字）：

超過長度就拆成多段再丟 TTS（Streamlit 也可以設 warning）

3.2 語言偵測 / 語者選擇（選擇性）

可以用簡單 heuristic：

若多數字元是中文 → language = "zh"

若多數字元是 A–Z → language = "en"

然後根據語言 map 到不同 voice / model：

zh → 女聲 A

en → 男聲 B
（具體取決於你選哪個 TTS 引擎）

3.3 輸出格式準備

常見輸出格式：

.wav（品質好、體積稍大）

.mp3（較小，適合網頁播放）

在 Streamlit 端：

需要 bytes 格式給 st.audio() 使用

可以設計檔名：

tts_output_20251201_120045.mp3

或只使用記憶體中的 BytesIO，不真正存檔

這一階段你就可以先在 Jupyter / VS Code 寫好：

clean_text(text)

synthesize(text, lang, voice, speed, pitch) -> audio_bytes

4. Modeling（建模）

這裡有兩個層次：

4.1 「不自己訓練」：直接用現成 TTS 模型（強烈建議作為第一版）

幾個常見選項：

gTTS（Google）

優點：簡單、能跑、多語言，pip 一行就用

缺點：需要網路、速度和品質不完全可控、商用要注意條款

from gtts import gTTS
import io

def tts_gtts(text, lang="zh"):
    tts = gTTS(text=text, lang=lang)
    fp = io.BytesIO()
    tts.write_to_fp(fp)
    fp.seek(0)
    return fp.read()  # bytes 給 Streamlit 播放


Coqui TTS / 🐸 TTS

優點：開源、自訓練/微調、可離線、可選多模型

缺點：安裝較重、可能需要 GPU

pyttsx3

優點：完全離線、跟系統內建語音引擎連動

缺點：聲音機器感重、多平台差異大

在 CRISP-DM 的 Modeling 階段，你要做的是：

選定 1～2 個 TTS 引擎，寫成統一介面：

def synthesize(text, engine="gtts", lang="zh", speed=1.0, pitch=0.0):
    # 根據 engine 分流
    # 回傳 audio_bytes


調整模型參數：

語速、音調

模型 / 語者 id（例如 Coqui 有不同 speaker）

對 test_sentences.csv 產生語音，實際聽一輪，做小記錄表：

id	engine	lang	speed	pitch	quality_note
1	gtts	zh	1.0	0.0	有點機器感
2	coqui	zh	0.9	0.1	最自然

這就等於是「模型實驗」，只是你不是在訓練，而是在 挑引擎 + 調參數。

4.2 「自己訓練 / 微調」（進階，可之後再做）

如果你想把這個專題變成論文 / 研究：

選一個開源架構（Tacotron2 / VITS / FastSpeech2 / Coqui TTS）

收集一套單一講者語音資料（1～10 小時）

做：

文本正規化

音素轉換（G2P）

對齊 / 訓練模型

然後把「推論程式」包成 synthesize()，照上面接口接進 Streamlit

這個就會變成一個完整「語音合成研究 + demo 網站」。

5. Evaluation（評估）

CRISP-DM 一定要做「模型評估」，在這個專案可以拆成：

5.1 技術面評估

延遲（Latency）：

文字長度 vs 產生時間

例如 50 字平均 0.8 秒、200 字平均 3.5 秒

穩定性：

連續產生 20 次不會 crash / timeout

可以簡單寫 script：

import time

def benchmark_engine(engine, sentences):
    times = []
    for s in sentences:
        t0 = time.time()
        _ = synthesize(s, engine=engine)
        times.append(time.time() - t0)
    return sum(times)/len(times), max(times)

5.2 人耳主觀評估（MOS, Mean Opinion Score）

可以請幾個人聽樣本，做一個簡單表單：

問題：

清晰度（1–5）

自然度（1–5）

停頓是否合理（1–5）

然後統計平均分數：

若某個引擎在中文 MOS 4.2，英文 MOS 3.8 → 可以寫入報告

5.3 Streamlit 端體驗評估

是否容易操作：

一眼看得出要在哪輸入文字、按哪個按鈕

是否容易調整參數：

語言、聲線、速度有沒有太複雜

是否支援：

播放

下載（st.download_button）

如果你要交報告，可以整理成一個小小的實驗結果章節。

6. Deployment（部署）
6.1 Streamlit App 架構設計

功能區塊可以這樣排：

標題 / 說明區

st.title("文字轉語音 / Text-to-Speech Demo")

簡介＋使用步驟

輸入區

st.text_area("輸入文字")

語言選擇：st.selectbox(["Auto", "中文", "English"])

引擎選擇（如果你有多引擎）：st.selectbox(["gTTS", "Coqui"])

語速 / 音調滑桿

按鈕

「產生語音」

輸出區

st.audio(audio_bytes, format="audio/mp3")

st.download_button("下載音檔", audio_bytes, file_name="tts_output.mp3")

6.2 最簡版範例架構（pseudo-code）
import streamlit as st
from gtts import gTTS
import io

st.title("文字轉語音 TTS Demo")

text = st.text_area("請輸入文字", height=150)
lang = st.selectbox("語言", ["自動判斷", "中文 (zh)", "English (en)"])
engine = st.selectbox("引擎", ["gTTS"])
speed = st.slider("語速（僅部份引擎有效）", 0.5, 1.5, 1.0, 0.1)

def detect_lang(text):
    # 簡單版：偵測中文或英文
    # 回傳 "zh" 或 "en"
    pass

def tts_gtts(text, lang_code):
    tts = gTTS(text=text, lang=lang_code)
    fp = io.BytesIO()
    tts.write_to_fp(fp)
    fp.seek(0)
    return fp.read()

if st.button("🔊 產生語音", type="primary"):
    if not text.strip():
        st.warning("請先輸入文字")
    else:
        if lang.startswith("自動"):
            lang_code = detect_lang(text)
        elif "中文" in lang:
            lang_code = "zh"
        else:
            lang_code = "en"

        audio_bytes = tts_gtts(text, lang_code)

        st.success("產生完成！")
        st.audio(audio_bytes, format="audio/mp3")
        st.download_button(
            label="下載 MP3",
            data=audio_bytes,
            file_name="tts_output.mp3",
            mime="audio/mpeg"
        )


這段只是結構示意，不是完整 code，但已經涵蓋 CRISP-DM 裡的「部署」概念。

6.3 部署位置

本機開發：streamlit run app.py

免費雲端：

Streamlit Community Cloud

Hugging Face Spaces + Gradio/Streamlit

內網 / 公司環境：

Docker 包起來 → 放到內部伺服器

部署時要注意：

如果用 gTTS，要有外網

如果用自訓 TTS 模型，要確認伺服器 GPU / RAM 夠